<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ('../includes/head') %>
    <title><%= query %> - Search - Snappy Book Review</title>
  </head>
  <body>
    <%- include('../includes/header') %>
    <div class="sbr-maincontainer">
      <% if (results.length===0) { %>
        <h1>Search - <%= query %> - No results</h1>
      <% }  else { %>
        <h1>Search - <%= query %></h1>
      <% } %>
      <% for (const result of results) { %>
        <p><a href="<%= config.siteUrl %>/book/<%= result.isbn %>"><%= result.fullTitle %></a></p>
      <% } %>
    </div>
    <%- include('../includes/footer') %>
  </body>
</html>